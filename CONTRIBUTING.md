# Contribución
1. Crear una rama `feature/nombre` desde `develop`.
2. Añadir o actualizar pruebas.
3. Ejecutar `npm run check`.
4. Abrir pull request hacia `develop`.
5. No fusionar si CI falla o si se elimina una función heredada.
