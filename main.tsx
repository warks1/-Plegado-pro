import {StrictMode,useEffect,useState} from 'react';import {createRoot} from 'react-dom/client';import {RouterProvider} from 'react-router-dom';import {router} from './app/router';import './styles.css';
function Boot(){const [ready,setReady]=useState(false);useEffect(()=>{const t=window.setTimeout(()=>setReady(true),5000);return()=>window.clearTimeout(t)},[]);if(!ready)return <div className="startup-splash"><img src="/assets/plegar-pro-industrial-logo.png" onError={e=>{(e.currentTarget as HTMLImageElement).src='/assets/brand-approved.png'}}/><h1>PLEGAR <span>PRO</span></h1><p>Professional Sheet Metal Bending Suite</p><div className="startup-progress"><i/></div><small>Inicializando geometría · bibliotecas · simulación · interfaz</small></div>;return <RouterProvider router={router}/>}
createRoot(document.getElementById('root')!).render(<StrictMode><Boot/></StrictMode>);
