import {useState} from 'react';import {Page} from '../../components/Page';import {useAppStore} from '../../store/useAppStore';
export function CustomersPage(){const {customers,addCustomer}=useAppStore();const [name,setName]=useState('');const [email,setEmail]=useState('');return <Page title="Clientes" subtitle="CRM técnico y contactos"><div className="two-col"><section className="panel"><h3>Alta de cliente</h3><form className="form" onSubmit={e=>{e.preventDefault();if(!name)return;addCustomer({id:crypto.randomUUID(),name,contact:'',email,phone:'',notes:''});setName('');setEmail('')}}><label>Empresa<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><button type="submit">Crear cliente</button></form></section><section className="panel"><h3>Directorio</h3><div className="rows">{customers.map(c=><article className="library-card" key={c.id}><strong>{c.name}</strong><span>{c.contact||'Sin contacto asignado'}</span><small>{c.email||'Sin email'} · {c.phone||'Sin teléfono'}</small></article>)}</div></section></div></Page>}
