import {Page} from '../../components/Page';import {useAppStore} from '../../store/useAppStore';
export function RequirementsPage(){const requirements=useAppStore(s=>s.requirements);return <Page title="Matriz de requisitos" subtitle="Estado honesto de las funciones acordadas"><section className="panel"><div className="requirements-table">{requirements.map(r=><article key={r.id} className={`requirement ${r.status}`}><div><b>{r.module}</b><strong>{r.label}</strong><small>{r.detail}</small></div><span>{r.status==='implemented'?'Implementado':r.status==='partial'?'Parcial':'Pendiente'}</span></article>)}</div></section></Page>}
