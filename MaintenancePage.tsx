import {useState} from 'react';import {Page} from '../../components/Page';import {useAppStore} from '../../store/useAppStore';
export function MaintenancePage(){const {maintenance,addMaintenance,selectedMachineId}=useAppStore();const [title,setTitle]=useState('Calibración general');const [dueDate,setDueDate]=useState(new Date().toISOString().slice(0,10));return <Page title="Mantenimiento" subtitle="Intervenciones y calibraciones"><div className="two-col"><section className="panel"><form className="form" onSubmit={e=>{e.preventDefault();addMaintenance({id:crypto.randomUUID(),machineId:selectedMachineId,title,dueDate,status:'planned',notes:''})}}><label>Intervención<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Fecha<input type="date" value={dueDate} onChange={e=>setDueDate(e.target.value)}/></label><button type="submit">Programar</button></form></section><section className="panel"><div className="rows">{maintenance.map(m=><article className="library-card" key={m.id}><strong>{m.title}</strong><span>{m.machineId} · {m.dueDate}</span><small>{m.status}</small></article>)}</div></section></div></Page>}
